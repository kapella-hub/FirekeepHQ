"""firekeep-client — Firekeep portable client kit.

`__version__` is the single shared version-tag anchor (spec §3). The client
runs a skew check against Cortex `/version` using this value.
"""

__version__ = "0.1.23"
