# gitleaks allowlist. Entries are FINGERPRINTS (commit:path:rule:line), so an
# entry stops applying the moment the line moves — a blanket rule-disable would
# hide the next real finding.
#
# Both suppressed findings are test fixtures. Verified, not assumed: one is a
# base64 string that decodes to an obvious English placeholder; the other is a
# sample client profile whose base_url points at a .example hostname. gitleaks
# is right to flag the SHAPE — the values are not credentials.
#
# The literal values are deliberately NOT reproduced here. The first version of
# this file quoted them to explain itself and gitleaks flagged THIS FILE,
# turning the allowlist into a new leak. Read the lines the fingerprints point
# at if you need to re-check them.
1fa0e9fa046ad2b6ff0d420a58e5676129e34197:tests/test_deploy_lib.py:generic-api-key:224
1fa0e9fa046ad2b6ff0d420a58e5676129e34197:client/tests/test_resolver.py:generic-api-key:128
#
# The two entries below are this file's own earlier version. It quoted the fake
# values while explaining them, gitleaks flagged the allowlist itself, and I
# removed them — but `gitleaks detect` scans FULL HISTORY, so commit 404fe35
# still contains that text and always will. A working-tree edit cannot unmake a
# committed finding; only an allowlist entry or a history rewrite can, and a
# force-push to erase two fake keys is not a trade worth making.
404fe35ec30385c9c84d825ab3cd46893cfdf023:.gitleaksignore:generic-api-key:6
404fe35ec30385c9c84d825ab3cd46893cfdf023:.gitleaksignore:generic-api-key:8
