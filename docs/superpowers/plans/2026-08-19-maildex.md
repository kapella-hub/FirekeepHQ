# Maildex Round 1 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development. The spec is authoritative: `docs/superpowers/specs/2026-08-19-maildex-design.md` — §2 invariants M1–M7, §3 mechanics, §4 tests. This plan records the task split and file ownership only.

**Server side: DONE** (4413ec4 + pin fix): KNOWN_DEX_IDS + dex:maildex scope + email source_type + all pins.

## Task W — the wheel (owner: agent, dir `maildex/` only)
`maildex/` package mirroring `docdex/` layout: pyproject (name firekeep-maildex, module firekeep_maildex, **zero third-party deps** — stdlib imaplib/email/html.parser; version 0.1.0; console script `firekeep-maildex = firekeep_maildex.cli:main`), LICENSE/NOTICE copied, src modules per spec §3 (accounts, imapio, parse, state, sync, wire, cli), full offline test suite per spec §4 incl. the EXAMINE-only/PEEK-only spy guard and the password-never-touches-disk spy. Vault access via the client kit's MCP call helper (read nightshift.py's `_mcp.call_tool` usage; `vault_retrieve`/`vault_store`/`vault_delete` are MCP tools). firekeep-client>=1.0.3,<2 dependency.

## Task I — client integration (owner: agent, `client/` + `dashboard/` untouched)
KNOWN_DEXES maildex entry in `client/firekeep_client/dexes.py` (kind ingest-client, indexes "email", import_probe firekeep_maildex); `firekeep maildex` bridge in cli.py (lazy import, cmd_docdex pattern); `maildexsync.py` trigger (docdexsync twin, registered+≥1 account+stale gates, same claim/spawn mechanics) wired into session_start after docdexsync's nudge; doctor `_check_dexes` maildex row (accounts · last sync · failures, local reads only); checkout install path adds sibling `maildex/` (cmd_install, docdex pattern); tests for all of it. Sole owner of cli.py/dexes.py/session_start.py/docdexsync-adjacent files.

## Task R — release wiring (owner: agent, bootstraps/workflows/root-tests)
The 4th wheel, exactly the docdex-B5 pattern + the single-resolution rule (the combined `uv pip install` line gains `${maildex_wheel}` — NEVER a separate step; test pins updated): install.sh/install.ps1 fetch+verify+probe (probe imports firekeep_maildex), make_release presence guard, release.yml build/copy/pypi leg (env `pypi-maildex`), test_bootstrap_maildex.py twin, ps1 fetch-count 7→8, venv-provisioning ordering twins, e2e fixture builds+serves the wheel, requirements-lock not-locked list, check_licenses exclusion, installlab dist.

## Mine after agents land
Independent verification (suites + M2/M3 read-through), dovecot e2e on the VPS (seed via APPEND from a seeding script; the wheel never writes), docs (dexes guide Maildex section w/ M5 disclosure, CLAUDE.md row, client-kit paragraph), gh env pypi-maildex, releases client-v1.1.0 + v1.1.0, site after release.
