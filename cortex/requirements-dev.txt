# Dev/test dependencies for FirekeepCortex.
# Runtime deps live in requirements.txt. Install both for the full host test run:
#   pip install -r requirements.txt -r requirements-dev.txt && pytest
pytest>=8.0,<9
pytest-asyncio>=0.24,<1
pytest-cov>=5,<8
fakeredis>=2.21,<3   # 42 test files use it; moved out of requirements.txt
httpx>=0.27,<1
ruff>=0.6,<1
